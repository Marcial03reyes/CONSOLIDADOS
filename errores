Fecha Inicio:
if(empty(items('Apply_to_each')?['Inicio']), null, formatDateTime(items('Apply_to_each')?['Inicio'], 'yyyy-MM-dd'))

Hora Inicio:
if(empty(items('Apply_to_each')?['Inicio']), null, formatDateTime(items('Apply_to_each')?['Inicio'], 'HH:mm'))

Fecha Fin:
if(empty(items('Apply_to_each')?['Fin']), null, formatDateTime(items('Apply_to_each')?['Fin'], 'dd-MM-yyyy'))

Hora Fin:
if(empty(items('Apply_to_each')?['Fin']), null, formatDateTime(items('Apply_to_each')?['Fin'], 'HH:mm'))
