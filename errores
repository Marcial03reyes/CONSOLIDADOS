if(
  or(empty(items('Apply_to_each')?['FechaInicio']), empty(items('Apply_to_each')?['HoraInicio']), empty(items('Apply_to_each')?['FechaFin']), empty(items('Apply_to_each')?['HoraFin'])),
  null,
  if(
    less(outputs('Fin_Real'), outputs('Inicio_Real')),
    0,
    div(sub(outputs('Fin_Real'), outputs('Inicio_Real')), 600000000)
  )
)
