if(
  or(empty(items('Apply_to_each')?['FechaInicio']), empty(items('Apply_to_each')?['HoraInicio'])),
  null,
  max(
    ticks(concat(items('Apply_to_each')?['FechaInicio'], 'T', items('Apply_to_each')?['HoraInicio'], ':00')),
    ticks(concat(items('Apply_to_each')?['FechaInicio'], 'T', first(outputs('Get_matriz')?['body/value'])?['Open'], ':00'))
  )
)
