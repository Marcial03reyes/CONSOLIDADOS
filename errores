if(empty(items('Apply_to_each')?['Hora Inicio']), null, concat(formatNumber(div(int(mul(float(items('Apply_to_each')?['Hora Inicio']), 1440)), 60), '00'), ':', formatNumber(mod(int(mul(float(items('Apply_to_each')?['Hora Inicio']), 1440)), 60), '00')))
if(empty(items('Apply_to_each')?['Hora Fin']), null, concat(formatNumber(div(int(mul(float(items('Apply_to_each')?['Hora Fin']), 1440)), 60), '00'), ':', formatNumber(mod(int(mul(float(items('Apply_to_each')?['Hora Fin']), 1440)), 60), '00')))
