@and(
  equals(length(split(item()?['Name'], '.')?[0]), 2),
  or(
    greaterOrEquals(int(split(items('Apply_to_each_year')?['Name'], '. ')?[1]), 2027),
    and(
      equals(int(split(items('Apply_to_each_year')?['Name'], '. ')?[1]), 2026),
      greaterOrEquals(int(split(item()?['Name'], '.')?[0]), 7)
    )
  )
)
