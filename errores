if(
  or(empty(items('Apply_to_each')?['FechaFin']), empty(items('Apply_to_each')?['HoraFin'])),
  null,
  min(
    ticks(concat(items('Apply_to_each')?['FechaFin'], 'T', items('Apply_to_each')?['HoraFin'], ':00')),
    ticks(concat(items('Apply_to_each')?['FechaFin'], 'T', outputs('Compose_Close_Ajustado'), ':00'))
  )
)
