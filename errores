@or(
  greaterOrEquals(int(items('Apply_to_each_year')?['Name']), 2027),
  and(
    equals(int(items('Apply_to_each_year')?['Name']), 2026),
    greaterOrEquals(int(split(item()?['Name'], '.')?[0]), 7)
  )
)
