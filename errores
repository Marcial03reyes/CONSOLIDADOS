#"Invocar función personalizada1" = Table.AddColumn(#"Archivos ocultos filtrados1", "Resultado", each try #"Transformar archivo"([Content])),
Marca_error = Table.AddColumn(#"Invocar función personalizada1", "TieneError", each [Resultado][HasError])
