let
    Agrupado = Table.Group(TuTablaConIDOrigen, {"IDOrigen"}, {{"Conteo", each Table.RowCount(_), Int64.Type}}),
    SoloDuplicados = Table.SelectRows(Agrupado, each [Conteo] > 1)
in
    SoloDuplicados
